{
  "devDependencies": {
    "vitepress": "^1.3.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev --open",
    "docs:build": "vitepress build --open",
    "docs:preview": "vitepress preview"
  }
}